# PinPoint bowling ball dataset (YOLO)
# No `path:` key — Ultralytics uses this file's folder as the dataset root
# (train/val paths resolve relative to this file regardless of working directory).
train: images/train
val: images/val

nc: 1
names:
  0: bowling_ball
